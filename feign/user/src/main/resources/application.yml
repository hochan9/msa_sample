spring:
  application:
    name: user-feign

  profiles:
    active: local

  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
server:
  port: 8080